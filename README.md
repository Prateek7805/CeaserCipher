# CeaserCipher  
The classic Ceaser Cipher with a mathematical representation C=P+K  
C=cipherText  
P=plainText  
K=key  
The code reads the "plainText.txt" file and requests a key which is an integer and produces the cipher  
The code then writes the cipherText in the file "cipherText.txt"  
